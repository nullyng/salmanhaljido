import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#E94560",
      contrastText: "#fff",
    },
    secondary: {
      main: "#16213E",
    },
  },
});

export default theme;
