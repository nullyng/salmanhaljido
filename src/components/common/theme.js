import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4d89c6",
      contrastText: "#fff",
    },
    secondary: {
      main: "#16213E",
    },
  },
});

export default theme;
